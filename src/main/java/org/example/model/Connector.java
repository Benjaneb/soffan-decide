package org.example.model;

/**
 * Represents a logical connector in the Logical Connector Matrix (LCM), which is combined with the Conditions Met Vector (CMV)
 */
public enum Connector { NOTUSED, ORR, ANDD }